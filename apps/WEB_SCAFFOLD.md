## apps/web

This contains the Next.js UI scaffold (chat, run trace, memory, eval dashboards).

Scaffold used:

```bash
cd apps
npx create-next-app@latest web --ts --eslint --tailwind --app --src-dir
```


